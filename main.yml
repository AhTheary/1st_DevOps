---

  - hosts: all
    become: true
    tags: common
    roles:
      - common

  - hosts: docker
    become: true
    tags: gitlab
    roles:
      - gitlab

  - hosts: web_server
    become: true
    tags: web_server
    roles:
      - web_server

  - hosts: docker
    become: true
    tags: docker
    roles:
      - docker


#   - hosts: all
#     become: true
#     tags: base
#     roles:
#       - base

